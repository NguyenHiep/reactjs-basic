"use strict";

$(document).ready(function () {
  var elemBody = $('body');
  
  // batch action click
/*  elemBody.find('.js-action-list-batch').eq(0).on('click', function () {
    var self = $(this),
			parent = self.parents('form').eq(0),
      bacth_action = elemBody.find('select[name="batch_actions"]').eq(0).val();
    if (bacth_action === '' || bacth_action === 'undefined') {
      alert('Vui lòng chọn tác vụ');
    } else {
			parent.submit();
    }
    return false;
  });*/
});